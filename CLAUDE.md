# Onyx — Obsidian Vault Web Viewer

[SosukeIto/my-vault](https://github.com/SosukeIto/my-vault) の Obsidian vault を Web 上で閲覧するアプリケーション。
Better-T-Stack(Bun + Turborepo + Hono + oRPC + TanStack Router)で構築する。

- 設計プラン: `docs/plan.html`
- デモ画面(デザイン基準): `docs/demo.html`
- 検証コマンド: `bun run check` / `bun run test` / `bun run build`

## プロジェクト決定事項(ユーザー判断、2026-08-23)

- **データベースは使わない**: 索引(リンク・バックリンク・タグ・検索)はメモリ上で同期ごとに再構築する。DB の追加を提案しない
- **対応端末は iPhone / Android / iPad / MacBook すべて**: phone(<640px)/ tablet(640〜1023px)/ desktop(≥1024px)の 3 クラス。検証は 390 / 820 / 1180 / 1440 幅のスクリーンショット
- **「一つ前に見ていたページに戻る」は必須**: ヘッダーの ← → ボタン + ブラウザ履歴
- **UI テキストは原則ゼロ**: ユーザーが読む文字は md の本文だけが理想。パネル見出し・ボタンラベル・ステータス文言は SVG アイコン + tooltip に置き換える。残してよいのはファイル名・本文・frontmatter の値・数字・設定画面のみ
- **git commit は Claude(オーケストレーター)が状況に合わせて行う**: 区切りごとに検証を通した状態でまとめて commit する。push はユーザーの指示があるときのみ

## マルチエージェント運用ルール

このリポジトリでの Claude の作業は**ハイブリッド方式**で行う。委譲は「並列で速くなる」か「コンテキストを守る」ときだけ使い、それ以外はメインセッションが直接書く。

- **オーケストレーター(メインセッションのモデル)**: 設計判断、タスク分解、成果物の検証、ユーザーへの報告を担当する。**下記の「直接実装の条件」を満たすタスクは自分で実装する**(往復コストの削減が目的。2026-08-22 改訂)
- **ワーカー(Agent ツールで `model: "opus"` を指定したサブエージェント)**: 「委譲の条件」を満たすタスクの実装を担当する
- **デザイナー(`.claude/agents/designer.md` の `designer` エージェント)**: アイコン・ビジュアルアセット・UI の見た目に関わる作業はすべてこのエージェントに委譲する(ユーザー指示: 2026-08-22)。絵文字アイコンは廃止し、designer が作る SVG コンポーネント(apps/web/src/components/icons/)を用いる

### 直接実装の条件(オーケストレーターが自分で書く)

以下を**すべて**満たすタスクは委譲せず直接実装する:

- 触るファイルが **3 つ以内**
- 他のタスクと並列に走らせる相手がいない(直列に 1 つずつ進む作業)
- 設計文書の作成・デザイン判断・設定変更・数行〜数十行の修正

理由: ワーカーは毎回コードベースを冷間スタートで読み直すため、1 タスクあたり 2〜4 万トークンと 3〜6 分の往復コストが乗る。直列タスクではこのコストに見合う利益がない。

### 委譲の条件(ワーカーを使う)

以下の**いずれか**を満たすときだけワーカーに委譲する:

- (a) **並列化できる**: 触るファイルが重ならない独立タスクが **2 つ以上**あり、同時に起動できる。この場合は必ず複数ワーカーを同時に起動する(直列に委譲しない)
- (b) **コンテキスト保護**: 1 タスクが大きく(目安: 4 ファイル以上、または新規モジュール一式)、メインセッションのコンテキストを圧迫する

ファイル競合がある作業同士は直列にする。

### 運用手順

1. オーケストレーターがタスクを分解し、各タスクを「直接実装」か「委譲」かに振り分ける
2. 委譲する場合は「完了条件付きの具体的な指示書」に落とし、(a) なら複数ワーカーを同時起動、(b) なら単独起動する
3. サブエージェントは作業後、変更点の要約を返す
4. **コミットはオーケストレーターが状況に合わせて行う**(ユーザー指示: 2026-08-23)。機能やドキュメントの区切りごとに、検証を通した状態で意味のある単位にまとめて commit する(push はユーザーの指示があるときのみ)。サブエージェントは commit しない
5. **検証ステップは方式を問わず必須**: 直接実装でも委譲でも、オーケストレーターが `bun run check` / `bun run test` / 必要に応じて `bun run build` や `bun run dev` での動作確認を行ってからユーザーに報告する。品質担保の主体はこの検証であり、委譲そのものではない

### サブエージェントへの指示に含めること

- 対象ファイルの絶対パスと、変更内容の具体的な列挙
- 完了条件(何が動けば/何が含まれていれば完了か)
- 「変更箇所の一覧を簡潔に返す」こと(成果物全文は返さない)
- サブエージェントは git commit を行わないこと(commit はオーケストレーターが行う)
